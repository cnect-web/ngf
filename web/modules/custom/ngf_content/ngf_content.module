<?php

use Drupal\Core\Link;
use Drupal\Core\Entity\EntityInterface;
use Drupal\Core\Entity\Display\EntityViewDisplayInterface;
use Drupal\views\Views;

/**
* Implements hook_entity_extra_field_info().
*/
function ngf_content_entity_extra_field_info() {
  $extra = [];

  $extra['node']['ngf_discussion']['display']['show_more'] = [
    'label' => t('Show more'),
    'description' => t('Displays link show more items'),
    'weight' => 100,
    'visible' => TRUE,
  ];

  return $extra;
}

/**
 * Implements hook_ENTITY_TYPE_view().
 */
function ngf_content_node_view(array &$build, EntityInterface $entity, EntityViewDisplayInterface $display, $view_mode) {
  if ($entity->bundle() == 'ngf_discussion' ) {
    // Show more link.
    if ($display->getComponent('show_more')) {
      $build['show_more'] = [
        '#type' => 'link',
        '#url' => $entity->toUrl(),
        '#title' => t('More')
      ];
    }
  }
}

/**
 * Implements hook_form_FORM_ID_alter().
 */
function ngf_content_form_comment_form_alter(&$form, &$form_state) {

  // Add a custom submit handler to act when the form submits.
  $form['#submit'][] = '_ngf_content_custom_comment_submit_handler';
//  var_dump($form);
//  exit();
}

/**
 * Custom submit handler for the comment form.
 */
function _ngf_content_custom_comment_submit_handler($form, &$form_state) {

//  var_dump('_ngf_content_custom_comment_submit_handler');
//  exit();
  // Redirect the user after submission
//  $nid = arg(2);
//  $form_state['redirect'] = 'node/' . $nid;
  $form_state->setRedirectUrl(\Drupal\Core\Url::fromRoute('<front>'));
}
